# tests/test_api_contracts.py
# API contract tests — runs against a live server in CI
# Full contract tests will be added in the next sprint.


def test_placeholder():
    """Placeholder — prevents CI api-test job from failing on missing file."""
    assert True
